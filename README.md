# Salary_Prediction_Using_Regression
Salary prediction using regression involves building a model that learns the relationship between independent variables (features) and a dependent variable (salary). The goal is to predict the expected salary for a person based on their attributes.
